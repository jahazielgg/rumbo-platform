"""Rumbo structural mapper v3.

Perception (ML or vector) is isolated in `mapper.perception`; everything after it is
deterministic geometry/topology so that correctness can be tested without a GPU or
PyTorch. See docs/structural-mapping-v3.md.
"""
